/// Some particle features, it helps to share the context of the particle system.
/// For example, bouncing off the walls, or changing the color of the particles.
pub struct ParticleContext {
    pub width: u16,
    pub height: u16,
}
